#include <stdio.h>

int main() {
   int array[6] = {9, 5, 8, 24, 2, 97};
   int i, maxItem;

   maxItem = array[0]; 
   for(i = 1; i < 6; i++) {
      if( maxItem < array[i] ) 
         maxItem = array[i];
   }
   printf("Phan tu lon nhat cua mang la: %d", maxItem);   
   return 0;
}
